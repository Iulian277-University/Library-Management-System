package actions;

/** The solutions from GetCommonBooksForRetailerIDs and GetAllBooksForRetailerIDs
 *  are already in linear time complexity, because they are using streams (filter and distinct),
 *  which have O(n) complexity: Internally, the methods are iterating through the lists
 */
public final class LinearComplexity {
    private LinearComplexity() {}
}
